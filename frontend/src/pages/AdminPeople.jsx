import React, { useEffect, useState } from 'react';
import client from '../api/client';
import Nav from '../components/nav';

export default function AdminPeople() {
  const [teams, setTeams] = useState([]); const [players, setPlayers] = useState([]); const [users, setUsers] = useState([]); const [requests, setRequests] = useState([]); const [message, setMessage] = useState('');
  const load = () => Promise.all([client.get('/api/platform/teams'), client.get('/api/platform/players'), client.get('/api/platform/admin/users'), client.get('/api/platform/admin/role-requests')]).then(([t, p, u, r]) => { setTeams(t.data.data || []); setPlayers(p.data.data || []); setUsers(u.data.data || []); setRequests(r.data.data || []); });
  useEffect(() => { load(); }, []);
  const notify = async (id) => { const title = window.prompt('Notification title'); const messageText = window.prompt('Message'); if (!title || !messageText) return; await client.post(`/api/platform/admin/players/${id}/notifications`, { title, message: messageText }); setMessage('Notification sent to player.'); };
  const editTeam = async (team) => { const name = window.prompt('Team name', team.name); const contact = window.prompt('Contact number', team.contact); if (!name || !contact) return; await client.patch(`/api/platform/admin/teams/${team._id}`, { name, contact }); setMessage('Team updated.'); load(); };
  const removeTeam = async (team) => { if (!window.confirm(`Delete ${team.name}? This cannot be undone.`)) return; await client.delete(`/api/platform/admin/teams/${team._id}`); setMessage('Team deleted.'); load(); };
  const role = async (user, value) => { await client.patch(`/api/platform/admin/users/${user._id}/role`, { role: value }); setMessage(`${user.name} is now ${value}.`); load(); };
  const review = async (id, status) => { await client.patch(`/api/platform/admin/role-requests/${id}`, { status }); setMessage(`Request ${status}.`); load(); };
  return <div className="min-h-screen bg-zinc-950 text-white"><Nav /><main className="mx-auto max-w-7xl px-4 pb-16 pt-28 sm:px-6 sm:pt-32"><p className="section-kicker">Organizer workspace</p><h1 className="section-title">Teams & players control centre.</h1>{message ? <p className="mt-5 rounded-lg bg-emerald-500/10 p-3 text-emerald-300">{message}</p> : null}<section className="mt-10"><h2 className="text-2xl font-bold">Coordinator requests</h2><div className="mt-4 space-y-3">{requests.filter((r) => r.status === 'pending').map((request) => <div key={request._id} className="flex flex-col gap-3 rounded-xl border border-zinc-800 bg-zinc-900 p-5 sm:flex-row sm:items-center sm:justify-between"><div><b>{request.user?.name}</b><p className="text-sm text-zinc-400">{request.user?.email} {request.note ? `· ${request.note}` : ''}</p></div><div className="flex gap-2"><button onClick={() => review(request._id, 'approved')} className="rounded bg-emerald-600 px-3 py-2 text-sm font-bold">Approve</button><button onClick={() => review(request._id, 'rejected')} className="rounded bg-zinc-700 px-3 py-2 text-sm font-bold">Reject</button></div></div>)}{!requests.some((r) => r.status === 'pending') ? <p className="mt-3 text-zinc-400">No pending coordinator requests.</p> : null}</div></section><section className="mt-12"><h2 className="text-2xl font-bold">Account designations</h2><div className="mt-4 overflow-x-auto rounded-xl border border-zinc-800"><table className="w-full min-w-[620px] text-left text-sm"><thead className="bg-zinc-900 text-zinc-400"><tr><th className="p-4">User</th><th>Email</th><th>Current role</th><th className="p-4">Change designation</th></tr></thead><tbody>{users.map((user) => <tr key={user._id} className="border-t border-zinc-800"><td className="p-4 font-semibold">{user.name}</td><td>{user.email}</td><td className="capitalize">{user.role}</td><td className="p-4"><select value={user.role} onChange={(e) => role(user, e.target.value)} className="rounded bg-zinc-950 p-2"><option value="player">Player</option><option value="coordinator">Coordinator</option><option value="admin">Admin</option></select></td></tr>)}</tbody></table></div></section><section className="mt-12 grid gap-8 lg:grid-cols-2"><div><h2 className="text-2xl font-bold">Team management</h2><div className="mt-4 space-y-3">{teams.map((team) => <div key={team._id} className="rounded-xl border border-zinc-800 bg-zinc-900 p-5"><b>{team.name}</b><p className="mt-1 text-sm text-zinc-400">{team.game?.name} · {team.players?.length || 0} players</p><div className="mt-4 flex gap-2"><button onClick={() => editTeam(team)} className="rounded bg-zinc-700 px-3 py-2 text-sm font-bold">Edit</button><button onClick={() => removeTeam(team)} className="rounded bg-red-700 px-3 py-2 text-sm font-bold">Delete</button></div></div>)}</div></div><div><h2 className="text-2xl font-bold">Player notifications</h2><p className="mt-2 text-sm text-zinc-400">Send a direct in-app notification to any registered player.</p><div className="mt-4 space-y-3">{players.map((player) => <div key={player._id} className="flex items-center justify-between rounded-xl border border-zinc-800 bg-zinc-900 p-4"><span>{player.IGN || player.name}</span><button onClick={() => notify(player._id)} className="rounded bg-red-600 px-3 py-2 text-sm font-bold">Notify</button></div>)}</div></div></section></main></div>;
}
